using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SlicedMesh : MonoBehaviour
{
    CustomMesh mesh;
    static void GenerateLeftMesh(MeshFilter oldMesh, List<Vector3> points, List<Vector3> intersectionPoints, Plane plane)
    {

    }

    static void GenerateRightMesh(MeshFilter oldMesh, List<Vector3> points, List<Vector3> intersectionPoints, Plane plane)
    {

    }
}
